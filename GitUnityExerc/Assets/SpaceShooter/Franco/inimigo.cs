using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class inimigo : MonoBehaviour
{
    int eu = 1;
    // Start is called before the first frame update
    void Awake()
    {
        contInimigoF1.contInimigo += 1;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
